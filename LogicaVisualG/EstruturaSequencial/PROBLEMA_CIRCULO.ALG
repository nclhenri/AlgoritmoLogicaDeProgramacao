Algoritmo "problema_circulo"

Var

   r, area : real

Inicio

      escreva("Digite o valor do raio do circulo: ")
      leia(r)

      area <- pi * exp(r, 2)
      escreva("Area = ", area:1:3)

Fimalgoritmo